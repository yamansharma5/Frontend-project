names= "sharma";
age = 43;
post = "sde2"
a = true;
b = false;


console.log("names");//if this then js will print names only because its inside quotes which means its a value

console.log(names, age,post,a,b);
// if we change values of above variables then we can js will take up the latest value
names="yaman sharma";
console.log(names);
//we cannot use certain reserved words like console in variable name but we can use Console
// try to write in camel case when two words in variable fullName
//js is a dynamically types language as we do not have to list about the variable that it is number ,integer, char,etc it will automatically idenfied by the interpretor

